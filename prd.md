# 제품 요구사항 정의서 (PRD) - 한자 마스터 (한자 검정 능력시험 도전하기)

본 문서는 초등학생부터 성인까지 다양한 연령층이 한자 검정 능력시험의 배정한자를 즐겁고 효과적으로 학습할 수 있도록 하는 게임형 학습 웹 애플리케이션의 세부 기능 사양과 구현 상태를 정의합니다.

---

## 1. 개요 (Overview)

### 1.1 서비스 목적 및 비전
- **목적:** 지루하고 주입식인 한자 암기 방식에서 탈피하여, 즉각적인 시각적 반응과 게임 요소를 통해 어문회 등 주요 급수별 한자를 완벽히 체득하도록 돕습니다.
- **비전:** 속도감 있는 **스피드 퀴즈**와 긴장감 넘치는 **한자비 맞추기 게임**, SVG 기반 획순 드로잉 시뮬레이션의 **한자 쓰기 연습**을 결합하여 친숙하고 전문적인 학습 경험을 제공합니다.

### 1.2 핵심 브랜딩 및 디자인
- **메인 화면 상단 브랜딩:** 최상단에 대표 아이콘과 함께 **한자 마스터** 타이틀이 배치되며, 그 아래 작은 글씨로 **""한자 검정 능력 시험에 도전 하세요!!""** 슬로건을 상시 노출하여 학습 욕구를 고취시킵니다.
- **비주얼 톤 앤 매너:** 눈이 편안하면서도 생동감 있는 **밝은 에메랄드/민트 그린 빛(약 80%) 중심의 라이트 테마**.
- **요소:** 깔끔한 화이트/민트 그라데이션 배경, 싱그러운 포레스트 그린 테두리, 부드럽고 직관적인 UI 요소.

---

## 2. 주요 기능 및 구현 사양 (Core Features)

### 2.1 플래시카드 연습 (Flashcards)
* **급수별 학습:** 어문회 배정한자 자료인 `all_hanja.xls` 기반으로 8급부터 4급까지 총 9개 급수(8급, 준7급, 7급, 준6급, 6급, 준5급, 5급, 준4급, 4급)를 지원합니다.
* **자연스러운 낭독 (Natural TTS):** 
  * 기계음과 같은 끊김을 방지하기 위해 뜻과 음 사이에 **쉼표 호흡(Comma Breath Pause)**을 강제로 주입하여 **"뜻, (자연스러운 멈춤) 음"** 형태로 부드러운 한국어 억양을 재현합니다.
  * Youdao 및 Google Translate Cloud TTS API를 우선 활용하며, 네트워크 단절 시 브라우저 내장 `speechSynthesis`로 유연하게 복구하는 멀티레이어 폴백 엔진을 적용하여 여성 음성으로 또렷하게 낭독합니다.
* **2단계 복습 테스트 (뜻/음 가림막 & 3초 시간제한):**
  * 1차 학습 루프가 완료되면 자동으로 2차 복습 테스트 화면으로 전환됩니다.
  * 한자 카드만 먼저 보여주고 **"뜻과 음을 맞춰보세요..."** 메시지와 함께 **3초 카운트다운 타이머 바**가 시각적으로 줄어듭니다.
  * **비동기 타이머 및 음성 콜백 가드 (`currentIndexRef`):** 사용자가 이전 카드를 수동으로 넘기거나 자동 전환이 발생할 때, 비동기 브라우저 음성 합성(SpeechSynthesis)의 종료 콜백이나 만료된 타이머가 뒤늦게 실행되어 다음 카드를 임의로 스킵하지 않도록 **모든 타이머 및 콜백 실행 시점에 카드의 고유 인덱스를 검증(Guard)**하는 구조를 적용해 버그 없이 안정적인 3초 대기 흐름을 유지합니다.
  * 3초가 지나면 뜻과 음을 카드에 노출하고 음성으로 1회 낭독한 뒤, 낭독 종료 1초 후에 다음 한자로 자동 전환됩니다.

### 2.2 스피드 퀴즈 (Speed Quiz)
* **방식:** 중앙에 한자 글자가 출제되면, 하단에 5가지 뜻/음 보기 단추(5지선다)가 나타납니다.
* **시간 제한:** 문제당 5초의 타이머 바가 줄어들며, 시간 초과 시 오답 처리됩니다.
* **출제 문항 수 & 랜덤 셔플:** 선택한 급수의 배정한자 중에서 **할 때마다 랜덤으로 셔플된 50문제**만 출제되어 지나친 문항 수로 인한 피로감을 없애고 균형 잡힌 도전을 제공합니다.
* **사운드 집중:** 사용자의 퀴즈 몰입도와 집중 속도를 극대화하기 위해 **음성 낭독은 완전히 제외**하고, 정답/오답 및 콤보 관련 경쾌한 효과음만 작동합니다.
* **골드 보상:** 퀴즈 완료 시 정답을 맞춘 문항당 **2 Gold, 5 XP**의 수련 보상이 즉시 누적 지급됩니다.

### 2.3 한자비 맞추기 게임 (Hanja Rain Game)
* **게임 방식:** 화면 상단에서 하늘색 한자비가 임의의 가로 위치와 무작위 속도로 아래로 떨어집니다.
* **출제 문항 수 & 랜덤 셔플:** 어떤 급수든 무작위로 엄선 및 셔플된 **50개의 한자비 빗방울**만 등장한 후 게임이 자동 종료(승리/패배)됩니다.
* **동적 보기 카드 순환 및 위치 이동 애니메이션:** 
  * 하단의 **10가지 뜻카드**는 매번 또는 시간 초 단위로 바뀌지 않고, 하늘에서 **새로운 한자비 단어가 3번째 단위로 새로 스폰되어 내려올 때만** 랜덤으로 재배치되도록 조절하여 난이도를 현실적으로 완화했습니다.
  * 셔플이 발생할 때 단순 텍스트 교체가 아닌 **카드 박스 자체가 화면 상에서 각자 제자리를 찾아 부드럽게 날아가듯 이동(Sliding & Bouncing transition)**하는 시각 효과를 제공합니다.
  * 새로운 한자가 출현할 때 정답 카드가 하단 그리드에 배치되어 있지 않은 경우, 다른 위치의 카드를 전혀 흩뜨리지 않고 제자리에서만 정답 카드로 교체해 준 뒤, 3번째 스폰 단위 시점에만 일괄적으로 셔플링을 적용하여 게임의 난이도 균형을 완성했습니다.
* **시각적 경고 시스템 (Danger Warning):** 
  * 빗방울(한자)이 화면 최하단(바닥)에 다가갈수록 **글자 색상이 빨간색으로 변경되며 긴박하게 깜빡(Blinking)거리기 시작**하여 사용자에게 시각적으로 놓칠 수 없는 긴장감을 부여합니다.
* **게임 종료 연출:** 게임이 종료되면 화면 하단의 10개 뜻카드를 깨끗하게 지워(Clear), 최종 점수판과 재도전 화면에 시각적 시선을 집중시킵니다.
* **사운드 구성:** 스피드 퀴즈와 동일하게 **낭독 음성은 제거**하고 빗방울 생성 및 타격 효과음만을 제공합니다.
* **골드 보상:** 게임 종료 시 클리어(파괴)한 빗방울 개수당 **2 Gold, 5 XP**의 수련 보상이 지급됩니다.

### 2.4 한자 쓰기 연습 (Hanja Writing Practice)
* **급수 제한 해제 & 통합 검색:** 
  * 특정 급수에 갇히지 않고 **전체 급수(8급~4급)의 배정한자**를 한 화면에 통합 노출합니다.
  * 급수별 배정한자가 많아 생기는 불편함을 방지하기 위해 **한글 음(훈음의 소리) 검색 창**을 제공합니다. 검색어 입력 시 해당되는 한자들을 실시간으로 필터링하여 궁금한 한자만 편리하게 추려볼 수 있습니다.
* **획순 벡터 애니메이션 (Stroke Simulation):**
  * 한자를 단순히 빨간 점으로 시뮬레이션하던 방식을 폐기하고, SVG 데이터를 활용해 실제 한자를 획 하나하나 붓으로 선을 긋듯이 **정확한 획순 애니메이션**을 구현했습니다.
* **집중을 위한 음성 제거:** 쓰기 획순에 시각적으로 온전히 집중할 수 있도록 한자를 읽어주는 오디오 음성은 제외했습니다.

### 2.5 글로벌 피드백 위젯 (Global Feedback Widget)
* **상시 노출 및 접근성:** 메인 화면 및 게임, 암기 학습 등 전체 화면 어디에서든 항상 화면 오른쪽 아래에 눈에 띄는 둥근 에메랄드색 챗봇(로봇) 모양 버튼(Floating Action Button)이 위치합니다. 버튼 왼쪽에는 "불편한 건 알려주세요! 💬" 문구의 말풍선이 부드러운 위아래 플로팅 애니메이션과 함께 항상 노출됩니다 (피드백 패널이 열리면 숨겨짐).
* **통합 피드백 패널:** 버튼 클릭 시 슬라이딩으로 열리는 피드백 패널을 통해 아래 항목을 입력할 수 있습니다:
  * **피드백 유형 선택:** 기술적 오류 (🐛), 사용상 불편사항 (😕), 개선 및 건의사항 (💡) 중 선택.
  * **내용 및 연락처:** 텍스트 필드 및 답변 수신을 위한 이메일/전화번호 기입(선택 사항).
* **데이터 보존:** 전송 성공 시 수련원생들의 피드백 정보가 브라우저의 `localStorage`('hanja_feedbacks')에 로깅되어 안전하게 적재됩니다.

### 2.6 익명 로그인 및 계정 연동 (Anonymous Auth & Account Linkage)
* **무가입 익명 시작:** 서비스 실행 시 복잡한 가입 절차나 개인정보 입력 없이 즉시 **익명 로컬 프로필**로 시작됩니다. 학습자는 아무런 조건 없이 학습 진도, 재화(Gold), 연속 학습일(Streak), 경험치(XP), 생명력(Hearts), 업적(Achievements) 등을 누적할 수 있습니다.
* **통합 계정 연동 및 로그인:** 메인 대시보드 중앙의 '계정 연동하기/로그인 하기' 영역을 통해 구글(Google) 및 카카오(Kakao) 로그인을 상시 지원합니다.
* **개인정보 미수집 프로필:** 사용자의 이메일이나 실명 등 어떠한 개인정보(PII)도 서버나 클라우드 데이터베이스에 수집하지 않으며, 이메일 주소를 **일방향 단방향 암호화 해시 키(`usr_숫자`)**로 변환하여 데이터를 매핑하는 개인정보 완전 미수집 아키텍처를 구현했습니다.
* **단어형 랜덤 닉네임 및 수동 변경 지원:**
  * 암호화 계정 생성 시 기계적이거나 불투명한 ID 대신 친근하고 흥미로운 **한국어 형용사+인물/사물 조합의 단어형 닉네임** 무작위 자동 발급합니다.
  * 기기 간 동기화 후 또는 언제든지 메인 대시보드 내의 닉네임 영역 옆 **수정 버튼**을 사용하여 사용자가 원하는 이름으로 자유롭게 실시간 변경 및 클라우드 동기화할 수 있습니다.
* **CORS 보증 클라우드 데이터베이스 연동 (`kvdb.io`):**
  * 여러 기기에서 데이터가 실시간으로 안전하게 복구 및 동기화될 수 있도록 **이메일 인증을 통과한 전용 CORS-허용 `kvdb.io` 데이터 버킷(`WPnA3ko81FraCfgWmNSzPM`)**을 구축했습니다.
  * 이를 통해 모든 기기(아이패드, 스마트폰, PC 등)에서 로그인 시 기존 수련생 기록이 실시간으로 동기화되며 동일한 닉네임이 그대로 계정에 복구됩니다.
* **연속 학습 Streak 자동 출석체크 조건 (1일 1회):**
  * 다음 조건 중 **최소 1가지**를 완료하면 1일 학습 출석이 자동으로 인정되고 Streak이 유지/연장되며 +30 XP의 출석 보상이 지급됩니다:
    1. **스피드 퀴즈** 1판(50문제) 완료
    2. **한자비 맞추기 게임** 1판(50문제) 완료
    3. **플래시 카드 연습** 50자 이상 학습 완료
  * 대시보드에 출석체크 진도가 실시간으로 안내됩니다.
* **골드 획득 제약 및 UI 안내:**
  * 남용 방지를 위해 **일반 쓰기 연습이나 출석체크로는 골드가 지급되지 않으며**, 오직 **스피드 퀴즈**와 **한자비 맞추기 게임** 플레이 결과에 따라 정량 획득할 수 있습니다.
  * 이와 관련된 재화 획득 안내 가이드가 대시보드 하단에 상시 배치되어 투명한 동기부여를 제공합니다.
* **모바일 및 다기기 레이아웃 최적화:**
  * 상단 헤더의 중복 닉네임 및 로그아웃 버튼을 제거하여 최상단 레이아웃을 완전히 비우고 로고와 메인 브랜드 텍스트만 표시되도록 심플화했습니다. 로그아웃 및 닉네임 표시는 대시보드 중앙의 전용 연동 카드 영역에 집중되어 화면 공간을 절약하고 시인성을 높였습니다.
  * 메인 및 대시보드 하단의 카피라이트 문구와 수련장 라벨(`© 2026 한자 마스터. All Rights Reserved. | 급수별 한자 게임 수련장 🏆`) 역시 모바일 및 태블릿 뷰포트에서 한 줄로 깔끔하고 대칭감 있게 상시 정돈되도록 디자인 사양을 개선했습니다.
* **수련생 등급 가이드 및 등급업 안내 모달:**
  * 대시보드의 현재 등급(서당 학도, 향시 통과, 진사 등) 표시 영역을 클릭 가능한 대화식 버튼 배지(이모지 포함)로 리모델링했습니다.
  * 이를 클릭 시, 화면이 흐려지는 고급 블러 필터와 함께 **"수련생 등급 가이드"** 안내창이 팝업됩니다.
  * 안내창에는 다음 등급까지 필요한 **누적 경험치 게이지 바**와 **최소 연속 학습일수 게이지 바**가 각각 백분율로 화려하게 노출되며, 최고 등급인 영의정 달성 시 축하 스파클 엠블럼이 표시됩니다.
  * 또한, 조선시대 관직 기반의 **세분화된 8단계 등급 체계**(서당 학도 📝 - 향시 통과 📜 - 진사 🎓 - 장원급제 🏆 - 사관 ✍️ - 한림학사 📚 - 판서 👑 - 영의정 🌟)와 각 등급별 최소 XP 및 최소 연속일수 요구사항, 등급별 묘사 정보가 표 형식으로 기재되며 사용자의 현재 등급에 `내 등급` 표식 및 하이라이트 효과가 적용됩니다.

---

## 3. 추가 보안 및 관리자 사양 (Security & Admin Console)

### 3.1 10분 세션 자동 로그아웃 (Session Inactivity & Visibility Guard)
* **보안 목적:** 다인 이용 장비나 공용 PC 등 다양한 학습 환경에서 로그인 세션이 무단 노출되거나 방치되는 것을 방지합니다.
* **무활동 감지 타이머:** 마우스 이동, 키 입력, 클릭, 스크롤, 터치 등 사용자의 유휴 상태(Idle)를 모니터링하여, **10분간 아무런 학습 관련 움직임이 없으면 자동으로 로그아웃** 처리합니다.
* **이탈 경과 시간 추적 (Tab Visibility Guard):** 
  * 사용자가 탭을 끄지 않더라도 브라우저 탭을 백그라운드로 전환하거나 다른 작업을 하는 등의 이탈 상황 발생 시, 이탈하는 순간의 타임스탬프를 기록합니다.
  * 다시 한자 마스터 탭으로 복귀하여 포커스를 활성화할 때, 백그라운드 체류 경과 시간이 **10분 이상인 경우 즉시 세션을 파기하고 자동 로그아웃**시킵니다.
* **피드백 연출:** 자동 로그아웃 시 대시보드로 즉각 리다이렉트되며, 한국어 음성 TTS 비서가 "장시간 미활동 또는 화면 이탈로 인해 자동 로그아웃 되었습니다."를 또렷이 안내하고 안내 경고창(Alert)을 표시합니다.

### 3.2 수련원 관리자 통제실 (Admin Console)
* **통제실 진입 및 데이터 클렌징:** 대시보드 최하단 푸터에 조화롭고 은밀하게 은폐 디자인된 `🛡️ 통제실` 링크를 제공합니다. 기존 로컬스토리지에 저장되어 캐싱되어 있던 구 버전 더미 피드백 데이터는 완전 소거(Cleansing)되어 클린한 상태로 가동됩니다.
* **수련생 가입 목록 더미 소거 및 실시간 연동:** 통제실 내 가입수련생 현황판에서 하드코딩되었던 가상의 더미 계정 3개를 영구 삭제하고, 클라우드 버킷의 실시간 사용자 프로필 명단만 동적으로 맵핑하여 진짜 이메일 주소 및 누적 점수를 깨끗하게 출력합니다. (학습 기록 로그 박스는 관리자 화면 가독성을 위해 완전히 제거되었습니다.)

### 3.3 한자비 맞추기 게임 자동 일시 정지 (HanjaRainGame Active Pause)
* **문제점 극복:** 백그라운드 탭으로 이탈했을 때 프레임 업데이트(`requestAnimationFrame`)는 멈추지만 스폰 타이머(`setInterval`)는 계속 돌아, 다시 복귀했을 때 한자비 빗방울들이 `y=0` 근처에 뭉쳐 굳어버리는 프리징 버그를 해결했습니다.
* **활성 감지 일시정지:** 탭 전환 등으로 화면을 벗어날 시 한자 스폰과 낙하 이동을 즉시 안전하게 멈추고, 화면 전체에 **⏸️ 게임 일시 정지됨** 블러(Blur) 오버레이 안내창을 띄워 게임을 동결시킵니다. 화면으로 돌아오면 자연스럽게 정지가 해제되어 이어서 플레이할 수 있습니다.
* **최신 점수 연산 동기화:** 퀴즈 콤보로 가속도가 붙을 때 발생할 수 있는 클로저 속도 연산 오류를 방지하기 위해, 항상 실시간 최신 점수를 반영하는 `scoreRef` 포인터를 연동하여 낙하 속도를 완벽하고 일관되게 정밀 통제합니다.

---

## 4. 기술 스택 (Technology Stack)

* **프론트엔드 (Frontend):** React + Vite
* **스타일링 (Styling):** Vanilla CSS 기반의 디자인 토큰 시스템
* **클라우드 데이터베이스 (Backend Sync):** CORS-Verified `kvdb.io` (Public Key-Value Store)
* **오디오 (Audio Engine):** Web Audio API 및 HTML5 Audio
* **벡터 애니메이션 (Stroke Engine):** SVG 및 획순 드로잉 애니메이션 컴포넌트

---

## 5. 최신 업데이트 내역 (Latest Updates)

### 5.1 실시간 글로벌 수련자 랭킹 (Real-time Global Leaderboard)
* **글로벌 동기화:** `kvdb.io` 클라우드 데이터베이스(`global_hanja_leaderboard` 키)를 활용하여, 전국에서 실제로 가입하고 로그인을 통해 정진 중인 진짜 수련생들의 누적 골드를 집계하고 실시간으로 연동합니다. 모든 수련자는 기기와 장소에 상관없이 완전히 동일한 실제 유저 랭킹표를 공유하게 됩니다.
* **더미 데이터 완전 제거:** 이황, 이이 등 가상의 라이벌 및 테스트 목적의 더미 데이터를 전체 제거하고, 오직 순수하게 직접 수집된 실 유저 데이터만을 랭킹 대상자로 선정하여 경쟁의 진정성을 확보했습니다.
* **비동기 로딩 스피너 UI:** 클라우드 데이터 호출 시 랭킹 새로고침 버튼에 Lucide `Loader2` 로딩 스피너 및 무한 회전 CSS 애니메이션 피드백을 추가하여, 네트워크 동기화 중임을 수련생들이 시각적으로 명확히 인지하게 조치했습니다.

### 5.2 한자비 맞추기 게임 프리징 및 y=0 멈춤 현상 완전 제거
* **오디오 자원 누수 완전 해결 (AudioContext Leak Fix):** 효과음 재생 시 생성되었던 `AudioContext` 객체들이 브라우저 자원 한계에 부딪혀 애니메이션 렌더러를 락(lock)시키는 문제를 차단하기 위해, 효과음 재생 완료 시점(0.2~0.4초 이내)에 강제로 `ctx.close()`를 처리해 브라우저 오디오 시스템의 자원 관리를 완전 안정화했습니다.
* **상태 업데이트 사이드 이펙트(Side Effect) 비동기 격리 (Deadlock 해결):** 60fps 업데이트 함수(`setFallingHanja` 내부 콜백) 및 한자 생성(`spawnHanja`) 내부에서 동시다발적으로 실행되던 외부 상태 변경(`setBottomCards`, `Lives` 감소, `Game Over`, `Victory` 판정) 및 사운드 발생 처리를 `setTimeout(..., 0)`으로 비동기 감싸 렌더 큐의 데드락을 완전히 예방했습니다.
* **컴포넌트 생명주기 및 0프레임 시작 멈춤 해결:** `gameState` 초기 상태를 `'ready'`로 정립하고 게임 루프 `useEffect`에 `hasStarted` 의존성을 올바르게 바인딩하여, 게임 시작 전 실행되던 백그라운드 프레임이 실제 게임 시작 시점의 타이머 초기화(`stopGameTimers`)에 의해 예외 없이 취소 및 중지되던 구조적 생명주기 버그를 완벽히 수정했습니다.
* **데스크탑(맥북/윈도우 PC) 브라우저 백그라운드 스로틀링 락 해결:** 맥북과 윈도우 기반 PC 환경에서 다른 탭으로 이동하거나 창 블러(blur) 시 브라우저가 전원/성능 관리 정책으로 `requestAnimationFrame` 실행을 멈추고 `setInterval`만 스로틀링하여 한자가 하늘(y=0)에 무더기로 굳은 채 복구되지 않는 현상을 해결하기 위해, `isPaused` 상태 변화에 맞춰 스폰 타이머와 애니메이션 렌더 프레임 루프를 물리적으로 해제 및 재생성하여 완벽히 재시작되도록 정교하게 수정했습니다. 이제 데스크탑 환경에서도 오동작 없이 부드럽게 낙하합니다.

### 5.3 글로벌 리더보드 동기화 및 닉네임 중복 방지 업데이트
* **Optimistic UI 오버라이드:** 게임이 끝난 직후 랭킹 리스트를 패치할 때 발생하는 서버(kvdb.io) 비동기 갱신 딜레이(약 0.5초~1.5초)를 상쇄하기 위해, 로컬에서 최종 획득한 골드 및 XP 점수를 리스트에 실시간 강제 매핑 및 재정렬하여 골드가 즉시 랭킹 순위표에 반영되도록 조치했습니다.
* **CORS Preflight (OPTIONS) 완전 우회:** `application/json` 전송 방식을 사용할 때 브라우저가 유발하는 CORS preflight(OPTIONS) 요청이 `kvdb.io` 무료 버킷에서 차단당하여 일반 사용자들의 랭킹 등록이 누락되던 문제를, 전송 헤더를 `Content-Type: text/plain` Simple Request로 변경하여 완전히 해소했습니다.
* **이메일 해시 고유 ID 매칭 및 닉네임 중복 노출 방지:** 닉네임을 변경할 때 기존 닉네임과 새로운 닉네임이 리스트에 두 번 겹쳐 노출되던 버그를, 이메일 해싱 고유 ID(`myId`) 기반의 단일 매칭으로 전면 전환하여 해결했습니다. 또한 이름 변경 시 랭킹판이 닉네임 의존성을 감지하여 즉각 한 줄로 깔끔하게 갱신되도록 최적화했습니다.

---

### 5.4 통제실 가입수련생 현황 실시간 클라우드 동기화
* **클라우드 감사 동기화:** 통제실(관리자 전용 패널)에서 "가입수련생 현황" 및 "상세 학습로그 타임라인"을 로딩할 때, 기존 기기의 브라우저 로컬스토리지(`hanja_local_users`)만 단순 조회하던 한계를 극복했습니다.
* **실시간 복구 감사:** 이제 관리자가 통제실에 진입하면 버킷 내의 모든 실제 사용자 키(`usr_*`)를 동적으로 스캔하고 병렬 fetch하여, 전국에서 실제로 가입/로그인 후 정진하고 있는 전체 실사용자 리스트 및 그들의 실제 학습 일지를 실시간으로 정확히 감독할 수 있습니다.

### 5.5 챗봇 건의사항(개선 및 오류제보) 통제실 열람 불가 현상 해결
* **챗봇-어드민 데이터 필드 호환성 정비:** 기존 챗봇 피드백 수집부(`FeedbackWidget.jsx`)에서 건의사항 내용을 `.content`, 분류를 `.type`, 비공개 연락처를 `.contact` 필드로 저장하던 구조와, 통제실 검토 모듈(`AdminPanel.jsx`)에서 `.body`, `.category`, `.author` 필드로만 조회하여 챗봇으로 제출된 건의사항 본문이 빈 상자로 뜨던 필드 불일치 문제를 완벽하게 해결했습니다.
* **유연한 매핑 오버라이드:** 이제 통제실은 피드백 인스펙터 진입 시 `body || content` 및 `category || type`, `author || contact`를 유동적으로 대조 판독하여, 챗봇을 통한 빠른 신고 건들도 누락 없이 완벽히 상세 본문을 렌더링하고 답변을 원격 저장할 수 있습니다.

### 5.6 플래시카드 오디오 엔진 전면 재설계 및 2회 반복 고정
* **오디오 인스턴스 완벽 격리:** 텍스트를 병합하여("쌀 미 쌀 미") 넘길 때 발생하는 억양 파괴를 방지하기 위해 1회용 음원 URL만 캐싱하고, 재생 시마다 **완전히 독립된 순수한 `new Audio()` 객체를 매번 새로 생성**하도록 오디오 엔진을 전면 교체했습니다.
* **비동기 타이머/콜백 꼬임 방어:** 재생 중 취소(Cancel) 또는 다음 카드로 넘어갈 때, 기존 오디오 객체의 `onended` 및 `onerror` 리스너를 즉각 `null`로 초기화하여 비동기 쓰레드가 백그라운드에서 좀비처럼 뒤늦게 콜백을 실행시켜 중복 재생하는 현상을 원천 차단했습니다.
* **정확한 2회 반복 통일:** "학습하기" 및 "한자맞추기(테스트)" 두 모드 모두에서 오디오가 정확히 2번씩만 낭독되도록 안정적인 재생 루프를 구축했습니다.

### 5.7 한자비 게임 난이도 완화 및 시각적 판정 최적화 (초등학생 눈높이)
* **하트 및 속도 개선:** 아이들이 쉽게 포기하지 않도록 기본 생명(하트)을 3개에서 **5개**로 상향하고, 한자가 내려오는 속도의 하한선과 최대 가속 상한선(0.5)을 크게 늦추어 여유로운 플레이 환경을 조성했습니다. 스폰 주기 또한 3초에서 4초로 지연시켰습니다.
* **동시 하강 한자 수 상한 제한 (Queue Lock):** 화면에 동시에 내려오는 한자의 수가 **최대 5개**를 넘지 않도록 통제했습니다. 한자를 맞추지 못해 5개가 누적되면 더 이상 한자가 생성되지 않고 Hold 상태로 대기하며, 사용자가 하나를 맞추거나 하나가 떨어져 4개 이하가 되는 즉시(다음 Animation Frame) 다음 빗방울이 재개되는 지능적 스폰 스로틀링을 적용했습니다.
* **바닥 충돌 임계치 보정:** 한자가 빨간 점선(82% 높이)에 살짝 걸칠 때 패배 처리되어 부자연스럽던 현상을, Y좌표 **89% 도달(빨간 선 아래로 한자가 완전히 통과한 시점)** 시 패배되도록 판정 임계치를 세밀히 보정했습니다.

### 5.8 한자 쓰기 연습 획순 애니메이션 100% 로딩 보장 (Multi-CDN Fallback)
### 5.9 가려진 한자 맞추기 게임 고도화 및 모바일 UI 최적화
* **유연한 오답 패널티:** 오답을 선택하더라도 즉시 다음 한자로 건너뛰거나 실패 처리되지 않고, 약 0.7초간 오답(X) 시각 피드백을 제공한 뒤 자동으로 다음 단계(더 넓은 시야)로 확장되어 재도전의 기회를 제공합니다. (단, 시야가 모두 열린 마지막 4단계에서 오답 시에만 최종 오답 처리됨)
* **모바일 6-그리드 선택지 최적화:** 한 번에 보기 쉽도록 6개의 뜻/음 카드를 3개씩 2줄(3x2 그리드)로 깔끔하게 정돈했으며, 모바일 화면에서 "뜻 음" 텍스트가 줄바꿈되지 않고 한 줄로 반듯하게 표기되도록 CSS를 수정하여 완벽한 반응형 뷰포트를 구축했습니다.

### 5.10 메인 대시보드 UI 간소화 및 직관성 향상
* **프로필 카드 군더더기 제거:** 복잡함을 줄이고 핵심 지표에 집중하기 위해 메인 화면 최상단의 프로필 프로그레스 바 영역에서 연속 출석(학습) 일수 표기를 과감히 삭제하고, 오직 **획득 경험치(XP), 누적 골드(G), 현재 등급** 3가지만 가장 명료하게 표출되도록 UI를 슬림화했습니다.
* **메뉴 명칭 직관성 강화:** 하단의 분석 영역 이름을 기존 '나의 학습 분석 및 오답노트'에서 **'수련 기록 로그 및 오답노트'**로 직관적이고 세계관에 맞게 일괄 변경했습니다.

### 5.11 연속 수련일(출석 체크) KST 시간 기준 완벽 동기화
* **표준시 독립 계산:** 해외 접속이나 디바이스 로컬 타임존(Timezone) 설정의 차이로 인해 자정 리셋이 어긋나 연속 학습일수가 초기화되던 현상을 해결하기 위해, 모든 출석 계산을 **한국 표준시(KST, UTC+9) 기준의 ISO 문자열 반환 함수(`getKstDateString`)**를 통과시켜 강제 동기화했습니다.
* **통합 출석 트리거:** 사용자가 **단 한 번이라도 골드나 경험치를 획득하는 의미 있는 게임 완주**를 할 경우, 즉시 백그라운드에서 KST 기준으로 어제와 오늘의 접속 여부를 판별해 정상적으로 연속 출석일에 +1 누적시켜 주도록 로직의 빈틈을 메웠습니다.

### 5.12 엣지 브라우저 애니메이션 프리징 해결 및 실시간 공지 팝업 시스템
* **`0` 타임스탬프 락(Lock) 무효화:** 엣지(Edge) 브라우저 등에서 `requestAnimationFrame`의 최초 진입 시 타임스탬프를 `0`으로 반환하여 애니메이션이 초기화되지 않고 한자가 화면 상단에 영원히 정지하던 치명적인 버그를, `useRef(null)` 명시적 null 검증 도입으로 완전히 철폐했습니다.
* **통제실 글로벌 긴급 공지 팝업 송출기 구축:** 관리자 통제실(Admin Console)에서 메인 화면에 띄울 팝업 메시지를 작성/삭제할 수 있는 제어 패널을 구축하고, `kvdb.io` 글로벌 저장소와 실시간 연동했습니다.
* **캐시 버스팅(Cache Busting)을 통한 실시간 반영률 100% 달성:** 브라우저가 공지사항 데이터를 몰래 캐싱하여 다른 기기에서 팝업이 늦게 뜨는 현상을 막기 위해, API 호출 시 `t=${Date.now()}` 및 `cache: 'no-store'` 파라미터를 강제 주입하고 사용자가 대시보드 화면에 진입할 때마다 즉시 새로고침 되도록 연결을 강화했습니다.

---

## 6. 회귀 방지 수호 규칙 (Regression Safeguards & Guardrails)
 
새 기능 요구나 리팩토링 시, 아래 핵심 로직은 **절대 수정하거나 삭제하지 않고 원본 구조를 유지**해야 합니다.

1. **[네트워크] `dbSync.js` 내 POST 전송 헤더 제약:**
   * **수호 규칙:** `saveProfileToCloud` 및 `updateGlobalLeaderboard`에서 `fetch` POST 요청을 보낼 때, 전송 헤더인 `'Content-Type': 'text/plain'`은 절대로 `'application/json'`으로 원복해선 안 됩니다. (CORS preflight가 유발되어 일반 사용자들의 실시간 동기화가 불가능해집니다.)
   * **데이터 구조:** 리더보드 데이터를 서버에 저장할 때 최상위가 배열 형태가 아닌 `{ board: currentBoard }` 객체 래퍼 형태로 전달되어야 KV 스토어 파서 에러를 예방할 수 있습니다.
2. **[게임 엔진] `HanjaRainGame.jsx` 스폰 비동기 디스패치 및 백그라운드 스로틀링 동기화 제약:**
   * **수호 규칙 (비동기 격리):** `spawnHanja` 내에서 `ensureHanjaInBottomCards`를 호출할 때 사용하는 `setTimeout(() => { ... }, 0)` 비동기 격리 구조는 절대로 동기 루프로 환원해선 안 됩니다. (React 18 상태 업데이트 충돌로 인해 한자비가 하늘(y=0)에 멈춰 서는 치명적인 버그가 재발합니다.)
   * **수호 규칙 (스로틀링 방지):** 데스크탑 브라우저의 백그라운드 탭 스로틀링 정책으로 인해 한자 객체들이 y=0에 얼어붙는 현상을 방지하기 위해, `isPaused` 상태에 맞춰 타이머와 애니메이션 루프 라이프사이클을 통제하는 독립 `useEffect` 구조를 훼손해서는 안 됩니다.
3. **[랭킹] `Leaderboard.jsx` 매칭 및 갱신 제약:**
   * **수호 규칙:** 사용자 매칭 시 닉네임이 아닌 이메일 단방향 암호화 해시 ID(`myId`)를 매칭 기준으로 삼는 방식을 유지해야 합니다.
4. **[통제실] `AdminPanel.jsx` 실시간 클라우드 감사 제약:**
   * **수호 규칙:** 통제실의 가입수련생 현황은 로컬 스토리지가 아니라 `getCloudAdminUsersList()` 비동기 클라우드 어그리게이터를 통해 전체 사용자 목록을 원격 로딩해야 합니다.
5. **[통제실] `dbSync.js` 내 이메일 전송 필드 보존 제약:**
   * **수호 규칙:** `saveProfileToCloud` 데이터 전송 본문에서 `email: profile.email` 정보를 지워서는 안 됩니다.
6. **[오디오] 불필요한 알림 TTS 보이스 영구 제거 유지:**
   * **수호 규칙:** 로그인, 로그아웃, 등급 가이드 시 음성 TTS는 제거되었으므로, 사용자 몰입과 개인 정보를 위해 추후 임의 복구를 금지합니다.
7. **[통제실] 챗봇 건의사항 및 게시판 건의사항 호환 매핑 제약:**
   * **수호 규칙:** 피드백 파싱 시 `{f.body || f.content}`와 `{f.author || f.contact}`, `{f.category || f.type}` 이중 매핑 구조를 반드시 유지해야 합니다.
8. **[오디오] 플래시카드 독립 `new Audio()` 인스턴스 락 (Audio Engine Lock):**
   * **수호 규칙:** 재사용 최적화를 핑계로 `new Audio()` 생성을 막아선 안 되며, 이벤트 종료 시 콜백을 즉각 `null`로 비우는 안전장치를 보존해야 겹침 재생 좀비 버그를 막을 수 있습니다.
9. **[게임 엔진] 한자비 게임 난이도 통제 및 최대 스폰 락 (Rain Game Guards):**
   * **수호 규칙:** Y 판정 89%, 기본 낙하 속도 증가(0.05~0.13), 속도 상한 0.8, 동시 화면 표출 한자수 `5개` 제한 스폰 제어문을 절대 삭제하지 마십시오. (답답하다는 유저 피드백 반영 사항)
10. **[한자 쓰기 엔진] 멀티레이어 CDN 폭포수 및 정규화(NFKC) 락 (Vector Stroke Fetch Lock):**
   * **수호 규칙:** 한자 획순 페치 루틴은 반드시 `normalize('NFKC')` -> 수동 이체자 매핑 -> 일반 CDN -> 번체자 CDN 2차 페치 과정을 직렬로 거쳐야 100% 로딩 보장이 무너지지 않습니다.
11. **[게임 엔진] `requestAnimationFrame` `0` 타임스탬프 락 (Edge Browser Zero-timestamp Bug Guard):**
   * **수호 규칙:** `HanjaRainGame` 메인 루프에서 초기 애니메이션 타임스탬프가 `0`으로 들어올 때 `!timestamp` 검사문이 무한 참(true)이 되는 것을 방지하기 위해 사용한 `useRef(null)`의 **명시적 null 형 변환 검사** 구조를 절대로 `useRef(0)` 구조로 회귀시켜서는 안 됩니다.
12. **[출석 로직] KST(한국 표준시) 정규화 강제 연산 (Timezone Integrity Lock):**
   * **수호 규칙:** `mockDb.js`와 `App.jsx` 내의 모든 날짜 기반 Streak(연속 학습일) 계산 로직에서 일반 `new Date().toISOString()` 사용을 전면 금지하며, 반드시 **`getKstDateString()`를 호출하여 KST(UTC+9)로 보정된 ISO 시간 포맷만 사용**해야 날짜 변경 오차를 막을 수 있습니다.
13. **[공유] 실시간 공지 송출 캐시 무력화 (Global Notice Cache Busting Lock):**
   * **수호 규칙:** `dbSync.js`의 `fetchGlobalNotice` 함수 내 GET 요청은 항상 브라우저 캐시에 감염되지 않도록 `?t=${Date.now()}` 파라미터와 `{ cache: 'no-store' }` 헤더를 반드시 동반해야만, 여러 장비 간의 실시간 업데이트 격차를 막고 팝업을 즉시 반영할 수 있습니다.
14. **[출석 로직] 클라우드 동기화 시 Streak 데이터 보존 락 (Cloud Sync Streak Lock):**
   * **수호 규칙:** `dbSync.js`의 `saveProfileToCloud`와 `loadProfileFromCloud` 페이로드에 반드시 `streakLastActive`, `lastActiveDate`, `flashcardsToday` 항목을 포함해야 합니다. 이를 누락할 경우 로그인/재로그인 시 유저의 연속 학습일 기준점(KST)이 삭제되어 무조건 스트릭이 1일로 리셋되는 치명적 버그가 재발합니다.
15. **[랭킹] 리더보드 실시간 동기화 락 (Leaderboard Sync Lock):**
   * **수호 규칙:** `Leaderboard.jsx`에서 로그인 유저의 랭킹 정보를 즉각 렌더링(Optimistic UI)할 때, 하드코딩된 경험치 구간이 아닌 메인 등급 판정 모듈(`getRankByXp`)을 사용하여 `profile.xp`와 `profile.streak` 두 조건을 모두 적용해야 합니다. 또한 데이터 갱신 `useEffect` 의존성 배열에 반드시 `profile.streak`를 포함시켜 연속 출석일 갱신 시 랭킹 보드도 실시간으로 리렌더링되도록 유지해야 합니다.
16. **[게임 엔진] 가려진 한자 게임 트랜지션 초기화 락 (Reveal Game Transition Reset Lock):**
   * **수호 규칙:** `HanjaRevealGame.jsx`에서 4단계(완전 노출)에서 다음 문제로 넘어갈 때, 이전 렌더링의 CSS `transition` 애니메이션 잔상이 남아 정답이 잠깐 스포일러되는 치명적인 시각 오류를 방지하기 위해, 한자가 렌더링되는 `div`에 반드시 `key={currentIndex}`를 부여하여 문제 전환 시 React DOM이 강제로 새로 마운트되도록 하는 안전장치를 영구 보존해야 합니다.
17. **[통제실] 건의사항 글로벌 클라우드 동기화 및 마이그레이션 락 (Feedback Cloud Sync Lock):**
   * **수호 규칙:** 챗봇(`FeedbackWidget.jsx`)과 게시판(`FeedbackBoard.jsx`)에서 접수되는 모든 건의사항은 로컬 스토리지가 아닌 `dbSync.js`의 `submitGlobalFeedback`을 통해 실시간으로 KV 클라우드로 전송되어야 합니다. 또한, 수련생들의 과거 로컬 데이터를 클라우드로 끌어올리기 위해 `App.jsx`에 심어둔 `syncLocalFeedbacks` 최초 1회 자동 병합(Migration) `useEffect` 로직을 영구 보존해야 합니다. (삭제 시 과거 피드백들이 관리자 통제실로 인입되지 않는 누락 사고가 발생합니다.)
